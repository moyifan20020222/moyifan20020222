可以以聚类，分类的方法，但是可以换一种解决方案，结构熵可以当作一种手段，方法可以不用这种，现在的聚类也有谱聚类的方法，换一种关键向量的获取方法不知道可不可行，至少实现还是挺简单的。
（如果配合着SAPT的方法的话，当前任务下的关键向量表示可能需要实时聚类得到，才能满足代码流程，最后的ARM模块，就直接把伪样本们与各个任务的聚类prompt进行比较好了，这里还有其他方法吗？）

PET块可以继续看，这里面分成了两种，一种是配合ViT这种图片分类的 ，
一种是处理NLP的，以LLaMA2，3这些的。
不过提示prompt 配合 大模型，可能耗时过久，验证麻烦